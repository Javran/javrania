* deploy:
    + `make deploy`
        * build `javrania`
        * build site
        * sync to submodule `javran.github.io`
        * commit submodule, use datetime as log message
* README.md
* index.md
